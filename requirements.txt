pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
arch>=6.0
matplotlib>=3.7
jupyter>=1.0

# Optional: only needed if you want to download fresh data from
# Kenneth French's Data Library instead of using the bundled CSV files.
# pandas_datareader>=0.10
